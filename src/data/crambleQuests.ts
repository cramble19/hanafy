import type { Quest } from '@/types'
import crambleTasks from './crambleTasks.json'

/** Edit this catalog to change Cramble's quests without touching page code. */
export const crambleQuests = crambleTasks as Quest[]
