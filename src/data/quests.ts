import type { Quest } from '@/types'
import hanaTasks from './hanaTasks.json'

/** Edit `hanaTasks.json` to add/remove Hana's quests without touching UI code. */
export const quests = hanaTasks as Quest[]
